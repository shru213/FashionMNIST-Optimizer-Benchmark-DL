# FashionMNIST-Optimizer-Benchmark-DL
A PyTorch deep learning project that builds a Multi-Layer Perceptron (MLP) to classify clothing items from the FashionMNIST dataset. The project focuses on benchmarking the convergence speed and accuracy of different optimization algorithms: Standard SGD, SGD with Momentum, and Adam.
